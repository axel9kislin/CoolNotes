<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:id="@+id/relativeLayout"
    tools:context="com.axel.coolnotes.AddNote">

    <LinearLayout
        android:id="@+id/layout_addTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/label_AddTitle"
            android:text="@string/str_title"
            android:textColor="#222"
            android:textSize="22dp" />
        <EditText
            android:id="@+id/userTitle_Add"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#222"
            android:textSize="20dp"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/layout_AddDesc"
        android:layout_below="@id/layout_addTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/label_AddDesc"
            android:text="@string/str_desc"
            android:textColor="#222"
            android:textSize="22dp"/>
        <EditText
            android:id="@+id/userDesc_Add"
            android:inputType="textMultiLine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#222"
            android:textSize="20dp"
            android:maxLines="99"
            android:minLines="1"
            android:lines="4"
            android:scrollbars="vertical"
            />
    </LinearLayout>
    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/placeholder"
        android:id="@+id/image_add"
        android:layout_below="@id/layout_AddDesc"
        android:layout_centerHorizontal="true"
        android:onClick="onImageViewAddClick"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_add"
        android:onClick="onAddNote"
        android:layout_below="@id/image_add"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        />

</RelativeLayout>
